# ====================================================================
#                CONFIGURAÇÕES DO AMBIENTE DE DESENVOLVIMENTO
# ====================================================================
#
# Este arquivo serve como um template para as variáveis de ambiente.
# Copie este arquivo para .env e preencha com os valores corretos.
#
# NUNCA adicione o arquivo .env ao controle de versão.

# --- Configuração do Django (OBRIGATÓRIO) ---
SECRET_KEY_DJANGO=sua-chave-secreta-django-aqui
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
DJANGO_SETTINGS_MODULE=smart_core_assistant_painel.app.ui.core.settings

# --- Credenciais do Google (OBRIGATÓRIO) ---
# Caminho para a chave de serviço do Firebase.
GOOGLE_APPLICATION_CREDENTIALS=src/smart_core_assistant_painel/modules/initial_loading/utils/keys/firebase_config/firebase_key.json

# ====================================================================
#                CONFIGURAÇÕES DOS SERVIÇOS REMOTOS
# ====================================================================
#
# As variáveis abaixo devem corresponder aos serviços definidos no
# docker-compose.yml do diretório `ambiente_base_dados`.

# --- PostgreSQL (Banco de Dados) ---
POSTGRES_DB=smart_core_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123
POSTGRES_HOST=192.168.3.127
POSTGRES_PORT=5436
# Define o modo de SSL: disable | prefer | require | verify-ca | verify-full
# No ambiente atual, "disable" foi necessário para conectar com sucesso.
POSTGRES_SSLMODE=disable
# Tempo máximo (em segundos) aguardando a conexão ao Postgres
POSTGRES_CONNECT_TIMEOUT=5

# --- Redis (Cache e Filas) ---
REDIS_HOST=192.168.3.127
REDIS_PORT=6382

# --- Ollama (Motor de IA) ---
OLLAMA_HOST=192.168.3.127
OLLAMA_PORT=11434
OLLAMA_BASE_URL=http://192.168.3.127:11434

# --- Evolution API ---
EVOLUTION_API_URL=http://192.168.3.127:8080
EVOLUTION_API_KEY=sua-chave-evolution-api-aqui

# ====================================================================
#                OUTRAS CONFIGURAÇÕES
# ====================================================================

# --- Configurações do Servidor ---
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# --- Webhook --- 
# O host do webhook deve ser acessível pela Evolution API.
WEBHOOK_URL=http://192.168.3.127:8000/oraculo/webhook_whatsapp/

# --- Modelos de Embeddings (PGVector + HNSW) ---
EMBEDDINGS_CLASS=OllamaEmbeddings
EMBEDDINGS_MODEL=mxbai-embed-large:latest
VECTOR_COLLECTION_NAME=treinamentos_mxbai_embed_large

# --- Parâmetros do Índice HNSW (pgvector) ---
HNSW_M=16
HNSW_EF_CONSTRUCTION=64
HNSW_EF_SEARCH=40