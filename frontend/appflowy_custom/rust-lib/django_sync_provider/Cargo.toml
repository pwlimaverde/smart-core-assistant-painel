[package]
name = "django_sync_provider"
version = "0.1.0"
edition = "2021"
description = "Provider de sincronização (AppFlowy ↔ Django adapter)."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"