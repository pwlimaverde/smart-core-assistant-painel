"""Tenant Admin do shell de Atendimento Unificado.

Sem registros próprios: o shell é apenas a ponte/UI. Os models de informação
foram consolidados em `atendimentos` e são registrados em
`atendimentos.tenant_admin` (refatoração modular v6.0).
"""
